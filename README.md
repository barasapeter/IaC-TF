# HandsOnTerraform
barasa::Terraform for IAC

## Basic TF commands
`terraform init` - to initialize the config
`terraform fmt` - fix formatting issues
`terraform validate` - checks syntax to ensure code is valid
`terraform plan` - look at all the resources i want to build and see what needs to be built
`terraform apply` - applies the config
